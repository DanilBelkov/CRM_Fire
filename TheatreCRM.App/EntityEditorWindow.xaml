<Window x:Class="TheatreCRM.App.EntityEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Карточка" Height="820" Width="860" MinHeight="640" MinWidth="780"
        Background="#101413" WindowStartupLocation="CenterOwner"
        BorderBrush="#1E2A26" BorderThickness="1">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#EEF5F1"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="#EEF5F1"/>
            <Setter Property="Background" Value="#17201D"/>
            <Setter Property="BorderBrush" Value="#2B3A35"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="9,7"/>
            <Setter Property="Margin" Value="0,4,0,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="8" 
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" 
                          Margin="4,0" 
                          VerticalAlignment="Top"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="#EEF5F1"/>
            <Setter Property="Margin" Value="0,4,0,12"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" 
                                      BorderBrush="{TemplateBinding BorderBrush}"
                                      Background="{TemplateBinding Background}"
                                      BorderThickness="{TemplateBinding BorderThickness}"
                                      Focusable="false"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Name="TemplateRoot" 
                                            CornerRadius="8" 
                                            Background="{TemplateBinding Background}" 
                                            BorderBrush="{TemplateBinding BorderBrush}" 
                                            BorderThickness="{TemplateBinding BorderThickness}">
                                            <Grid HorizontalAlignment="Right" Width="24">
                                                <Path Name="Arrow" 
                                                  Data="M 0 0 L 4 4 L 8 0 Z" 
                                                  HorizontalAlignment="Center" 
                                                  VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="true">
                                                <Setter TargetName="TemplateRoot" Property="Background" Value="#197b50"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter Name="ContentSite" 
                                          IsHitTestVisible="True" 
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                          ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="Left" 
                                          VerticalAlignment="Center"/>
                            <Popup Name="Popup" 
                               Placement="Bottom" 
                               IsOpen="{TemplateBinding IsDropDownOpen}" 
                               AllowsTransparency="True" 
                               Focusable="False" 
                               PopupAnimation="Slide">
                                <Grid Name="DropDown" 
                                  SnapsToDevicePixels="True" 
                                  MinWidth="{TemplateBinding ActualWidth}" 
                                  MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border Name="DropDownBorder" 
                                        Background="#197b50" 
                                        BorderThickness="1" 
                                        CornerRadius="0,0,8,8" 
                                        Margin="0,1,0,0">
                                        <ScrollViewer Margin="4" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="#EEF5F1"/>
            <Setter Property="Background" Value="#1D2925"/>
            <Setter Property="BorderBrush" Value="#2B3A35"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,9"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#21A66B"/>
            <Setter Property="BorderBrush" Value="#2CC77F"/>
            <Setter Property="Foreground" Value="#06110C"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style TargetType="Expander">
            <Setter Property="Foreground" Value="#EEF5F1"/>
            <Setter Property="Margin" Value="0,8,0,0"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <Border Name="Bd" CornerRadius="10" BorderThickness="{TemplateBinding BorderThickness}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            Background="{TemplateBinding Background}" 
                            SnapsToDevicePixels="true">
                            <ScrollViewer Padding="{TemplateBinding Padding}" Focusable="false">
                                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <DockPanel Margin="24">
        <!-- Нижняя панель кнопок -->
        <StackPanel x:Name="ButtonPanelView" DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0" Visibility="Visible">
            <Button x:Name="OkViewButton" Content="ОК" Click="OkViewButton_Click" Style="{StaticResource PrimaryButton}" MinWidth="120" Padding="18,9"/>
            <Button x:Name="EditButton" Content="Редактировать" Click="EditButton_Click" MinWidth="120" Padding="18,9" Margin="12,4,0,4"/>
        </StackPanel>
        <StackPanel x:Name="ButtonPanelEdit" DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0" Visibility="Collapsed">
            <Button x:Name="SaveButton" Content="Сохранить" Click="SaveButton_Click" Style="{StaticResource PrimaryButton}" MinWidth="120" Padding="18,9"/>
            <Button x:Name="CancelButton" Content="Отмена" Click="CancelEditButton_Click" MinWidth="120" Padding="18,9" Margin="12,4,0,4"/>
        </StackPanel>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock x:Name="HeaderText" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,18"/>

                <!-- Название -->
                <TextBlock Text="Название"/>
                <TextBox x:Name="TitleBox"/>

                <!-- Описание -->
                <TextBlock Text="Описание"/>
                <TextBox x:Name="DescriptionBox" AcceptsReturn="True" Height="88" TextWrapping="Wrap"/>

                <!-- ===== ПОЛЯ ДЛЯ НЕ-СЕКТОР ===== -->
                <StackPanel x:Name="NonSectorFields">
                    <!-- Инвентарный номер + Место хранения -->
                    <UniformGrid Columns="2">
                        <StackPanel x:Name="InventoryPanel" Margin="0,0,20,0">
                            <TextBlock Text="Инвентарный номер"/>
                            <TextBox x:Name="InventoryBox"/>
                        </StackPanel>
                        <StackPanel>
                            <TextBlock Text="Место хранения"/>
                            <TextBox x:Name="StorageBox"/>
                        </StackPanel>
                    </UniformGrid>

                    <!-- Состояние + Ответственный -->
                    <UniformGrid Columns="2">
                        <StackPanel Margin="0,0,20,0">
                            <TextBlock Text="Состояние"/>
                            <ComboBox x:Name="ConditionBox" Background="#FF17201D" BorderBrush="#FF2B3A35">
                                <ComboBoxItem Content="Новое"/>
                                <ComboBoxItem Content="Хорошее"/>
                                <ComboBoxItem Content="Требует ремонта"/>
                                <ComboBoxItem Content="Списано"/>
                                <ComboBoxItem Content="Другое"/>
                            </ComboBox>
                            <TextBox x:Name="ConditionReadOnlyBox" IsReadOnly="True" IsTabStop="False" BorderBrush="#1D2925" Visibility="Collapsed"/>
                        </StackPanel>
                        <StackPanel>
                            <TextBlock Text="Ответственный"/>
                            <ComboBox x:Name="ResponsibleBox" IsTextSearchEnabled="True" DisplayMemberPath="FullName" Background="#FF17201D" BorderBrush="#FF2B3A35"/>
                            <TextBox x:Name="ResponsibleReadOnlyBox" IsReadOnly="True" IsTabStop="False" BorderBrush="#1D2925" Visibility="Collapsed"/>
                        </StackPanel>
                    </UniformGrid>

                    <!-- Фото галерея -->
                    <TextBlock Text="Фото"/>
                    <Button x:Name="AddPhotoButton" Content="Добавить фото в галерею" Click="AddGalleryPhotoButton_Click" HorizontalAlignment="Left"/>
                    <ListBox x:Name="PhotosList" Height="96" Margin="0,6,0,12" Background="#FF17201D" BorderBrush="#FF2B3A35" ScrollViewer.HorizontalScrollBarVisibility="Auto"
                             MouseDoubleClick="PhotosList_MouseDoubleClick">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel IsItemsHost="True" Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="4" Padding="2" Background="#1D2925" CornerRadius="4">
                                    <Grid>
                                        <Image Width="64" Height="64" Stretch="UniformToFill" Source="{Binding}"/>
                                        <Button x:Name="DeletePhotoBtn" Content="×" Width="20" Height="20" Padding="0" Margin="0"
                                                Foreground="#EEF5F1" Background="#992B2B" BorderBrush="Transparent"
                                                HorizontalAlignment="Right" VerticalAlignment="Top"
                                                Tag="{Binding}" Click="DeleteGalleryPhoto_Click"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>

                <!-- Специфические поля (один стек, переключается IsReadOnly) -->
                <StackPanel x:Name="SpecificFields"/>


                <TextBlock Text="Привязки" FontSize="18" FontWeight="SemiBold" Margin="0,8,0,4"/>
                <!-- Связи в режиме просмотра -->
                <StackPanel x:Name="RelationshipsPanelView" Visibility="Visible"/>

                <!-- Связи в режиме редактирования -->
                <StackPanel x:Name="RelationshipsPanel"/>
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</Window>